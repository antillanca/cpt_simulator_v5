model_type: transformer
seed: 42
epochs: 3
batch_size: 64
lr: 0.0001
device: cuda
eval_every: 100
save_every: 500
train_split: 0.8
notes: Kaggle/GPU packaging profile only. Training remains optional.
